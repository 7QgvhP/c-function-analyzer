# 変更履歴 (Change Log)

本プロジェクト（C Function Analyzer）のすべての変更は、このファイルに記録されます。

## [2.7.0] - 2026-07-29
### 追加
* コピー時の出力形式を選べる機能を追加。画面上部の「コピー形式」で切り替えられます。
  * **変数名**: 変数名のみ（従来の動作、既定値）
  * **型名 + 変数名**: 型名とタブ区切りで変数名。表計算ソフトへ貼り付けると A列に型名、B列に変数名の2列になります
  * 個別コピー・一括コピーの両方に適用されます。
  * 呼び出し関数には型がないため、「型名 + 変数名」では型名の列が空になります（列の位置は揃います）。
  * 選択した形式はパネルを開いている間保持され、別の関数を解析し直しても維持されます。

### 変更
* 各項目に型名のデータ属性（`data-type`）を出力するようにしました。個別コピーボタンはコピー対象を親要素から取得するようになり、ボタン自身の `data-name` 属性は不要になりました。

## [2.6.1] - 2026-07-28
### 修正
* パネルの幅を狭くすると型名と変数名が2行に折り返して表示される問題を修正。表示領域に最小幅（480px）を設け、どの幅でも1行に収まるようにしました。
  * v1.14.0 のレスポンシブ対応で、幅400px以下のときに型名バッジを変数名の上へ折り返す指定が入っていたものを取りやめました。折り返すと視線が縦に散るうえ、型名バッジの固定幅による変数名の左端揃え（v2.0.1）も崩れるためです。
  * 最小幅に満たない場合は折り返さず横スクロールになります。変数名が収まらない場合は従来どおり末尾を省略表示します。

## [2.6.0] - 2026-07-28
### 追加
* 設定 `c-function-analyzer.includePaths` を追加。`#include "..."` を解決する際に追加で探索するディレクトリを指定できます。
  * 相対パスはワークスペースフォルダからの相対として解決し、絶対パス（`C:/SDK/include` など）はそのまま使用します。
  * 探索順は「インクルード元ファイルのディレクトリ」→「本設定のディレクトリ（記述順）」→「ワークスペースフォルダの直下」です。
  * 設定値は解決のたびに読み取るため、変更は VS Code の再起動なしで反映されます。

これにより、ヘッダを別ディレクトリにまとめている構成でも型と定義位置を解決できるようになりました。

```
proj/
├─ src/main.c        →  #include "types.h"
└─ include/types.h        includePaths: ["include"] を設定すると解決される
```

### 変更
* 探索候補パスの構築処理を `src/includePaths.ts` の純関数（`buildIncludeCandidates`）へ分離。ファイルシステムや VS Code API に依存しないため、ヘッドレスでテストできます。同一パスの重複も除去するようにしました。

## [2.5.0] - 2026-07-28
### 追加
* 構造体・共用体のメンバにアクセスしている場合、根元の変数の型ではなく**メンバ自身の型**を表示するようにした。
  * 例: `var_ptr->sub.member` は従来 `struct Outer*` と表示されていたが、`int` と表示されます。
  * ネストしたメンバ、構造体配列の要素（`tbl[5].id`）、アロー演算子、共用体、ポインタ引数のメンバ（`data->id`）に対応します。
  * 配列メンバは添字の有無に応じて表示を切り替えます（`cfg.name[8]` なら型は `char`、`cfg.name` なら型は `char[8]`）。
* 構造体・共用体の定義（`collectStructDefinitions`）をファイルスコープおよびインクルードファイルから収集するようにした。
  * タグ名（`struct Config`）と `typedef` 名（`HogeStruct`）の双方をキーとして登録し、どちらの表記からも解決できます。
  * `typedef struct Tag { ... } TagAlias;` のようにタグ名と別名の両方がある場合は、両方から引けます。
  * 変数宣言と同時に定義された構造体（`struct Data { int x; } global_data;`）にも対応します。

> 補足: 構造体定義が見つからない場合や、メンバ名が定義に存在しない場合は、従来どおり根元の変数の型を表示します。無理に解決するより、根元の型が見えている方が手がかりになるためです。関数ボディ内で定義されたローカルな型は収集対象外です。

## [2.4.0] - 2026-07-28
### 追加
* 分類ごと（入力変数・出力変数・内部変数・マクロ変数・呼び出し関数・マクロ関数）に、その分類の名前をまとめてクリップボードへコピーする「コピー」ボタンを追加。各分類の見出し右側、件数バッジの右に配置しています。
  * 変数と変数は**改行で区切られる**ため、表計算ソフトへ貼り付けると1つの変数につき1行になります。
  * コピーされる文字列は画面に表示されている名前です（配列は `hoge[N]`、呼び出し関数は `()` を除いた関数名）。
  * 項目が0件の分類にはボタンを表示しません。
  * 押下後は「完了」と表示し、1秒後に元へ戻ります（個別コピーボタンと同じ挙動）。

> 補足: 一括コピーは v1.10.0 で一度実装され、v1.11.0 で個別コピーボタンを追加した際に削除されていました。今回は当時の整形テキスト形式（`■ 入力変数` / `- hoge (int)`）ではなく、表計算ソフトへの貼り付けを想定した「名前のみ・1行1件」の形式で実装しています。

## [2.3.0] - 2026-07-28
### 変更
* 添字でアクセスされている配列の表示名に、**宣言された次元**を反映するよう変更。
  * `int hoge[N];` を `hoge[2]` や `hoge[i]` で使用している場合、従来の `hoge[]` に代わり `hoge[N]` と表示します。
  * 多次元配列は `grid[3][4]`、構造体配列のメンバは `tbl[5].id` のように表示します。
  * 添字の値によらず同じ表示名になるため、同一変数への複数のアクセスは従来どおり1件に集約されます。
  * 宣言の次元が特定できない場合（`extern int buf[];` やポインタへの添字アクセス）は、従来どおり `[]` と表示します。
* 配列の次元は**名前と型名のどちらか一方にのみ**表示するよう整理。
  * 添字でアクセスされている項目は名前側に出し、型名は `int` とします。
  * 添字なしで参照されている項目（`memcpy(dst, table, n)` など）と内部変数は、従来どおり型名側に `int[8]` のように出します。
* ハイライト検索の正規表現を、**添字の中身によらず任意の添字にマッチする**よう変更。表示名が `hoge[N]` でもコード上の `hoge[2]` や `hoge[i]` を検索できます。次元が式である場合（`buf[MAX + 1]`）にも対応します。

> 補足: 内部変数の名前には配列の次元を付けません。内部変数の名前は宣言名そのもの（`hoge_array`）であり、ここに `[]` を付けると `memcpy(dst, hoge_array, n)` や `sizeof(hoge_array)` のような添字を伴わない参照がハイライトから漏れるためです。配列であることは型名（`int[5]`）で示します。

## [2.2.1] - 2026-07-28
### 修正
* 配列として宣言された変数（例: `int hoge_array[5];`）の型が `int` と表示され、配列であることが分からない不具合を修正。次元とサイズを含めて `int[5]` のように表示します。
  * 対象は内部変数（ローカル変数）とグローバル変数です。多次元配列は `int[3][4]`、サイズ省略は `char[]`、マクロ定数によるサイズは `char[MAX_LEN]`、ポインタ配列は `int*[8]` と表示します。
  * 宣言子の解析で配列の次元は取得していましたが、変数宣言の収集側で型名に反映していませんでした（v1.16.1 の実装漏れ）。
* 引数の配列は従来どおりポインタ表記（`int*`）のままとします。C言語の仕様上、引数の配列はポインタへ減衰するためです。

> 補足: 配列であることを示す `[]` は型名側にのみ付与します。名前フィールドはハイライト検索とコピーの対象を兼ねるため、そこに `[]` を付けると `memcpy(dst, arr, n)` や `sizeof(arr)` のような添字を伴わない参照がハイライトから漏れます。名前側の `[]` は従来どおりアクセスパス（添字でアクセスされたこと）を表します。

## [2.2.0] - 2026-07-28
### 追加
* `#include "..."` を再帰的に辿り、インクルードファイル内で宣言されたシンボルを解決する機能を追加。
  * ヘッダの `extern` 宣言からグローバル変数の型を解決し、従来 `global (推定)` と表示されていた項目を実際の型（`int`、`struct Config` など）で表示します。
  * ヘッダ内の定義位置へ「定義へ」ボタンでジャンプできます。
  * ヘッダのプロトタイプ宣言から、呼び出し関数の定義位置を解決します。
* `#define` の定義を解決し、マクロ変数の型名バッジに定義値を表示する機能を追加（例: `macro (推定)` → `macro (100)`）。値を持たないマクロは `macro` と表示します。マクロ関数も定義位置へジャンプできます。
  * 解析対象ファイル自身に書かれた `#define` も対象です（従来は同一ファイル内でも `macro (推定)` のままでした）。

### 修正
* インクルードガード（`#ifndef HEADER_H`）や条件コンパイル（`#if` / `#elif` / `#else`）の内側にある宣言が、ファイルスコープの宣言として収集されない不具合を修正。
  * これらの宣言は AST 上でプリプロセッサ条件ブロックの子になるため、ルート直下のみを走査する従来の実装では拾えていませんでした。C言語ヘッダのほぼ全てがインクルードガードを使うため、この修正なしではインクルード探索が実質機能しません。
  * 条件の真偽は評価せず、記述されている宣言をすべて収集します。

### 探索の仕様
* 対象は `#include "..."` のみで、システムインクルード（`#include <...>`）は探索しません。
* 探索パスは「インクルード元ファイルのディレクトリ」→「各ワークスペースフォルダの直下」の順です。
* 再帰の深さ上限は8段、循環インクルードは検出して一度だけ展開します。
* 解析対象ファイル自身の宣言を最優先し、インクルード側は未登録のシンボルのみを補います。
* 解決できないインクルードや読み込みエラーは無視して解析を継続します。
* パース結果はファイルの更新時刻とあわせてキャッシュし、同一セッション中の再パースを避けます。

> 制約: 探索パスの都合上、`src/foo.c` から `#include "types.h"` と書かれていて実体が `include/types.h` にある構成は解決できません。また、ファイルは保存済みの内容を読み込みます。`typedef` の展開は行いません。

## [2.1.0] - 2026-07-27
### 追加
* 変数・関数の定義位置へジャンプする「定義へ」ボタンを追加。項目にマウスホバーすると「コピー」ボタンの左に表示され、クリックするとエディタで定義行を開いてカーソルを移動します。
  * 対応する項目: 引数、ローカル変数、同一ファイル内で宣言されたグローバル変数、同一ファイル内で定義・宣言された呼び出し関数。
  * 定義位置を特定できない項目（`global (推定)`、`macro (推定)`、`戻り値 (return)`、標準ライブラリ関数など）にはボタンを表示しません。
  * 関数の定義とプロトタイプ宣言の両方がある場合は、定義側の位置を優先します。

### 変更
* 解析結果の `calledFunctions` / `macroFunctions` を、関数名の文字列配列から名前と定義位置を持つオブジェクトの配列（`FunctionInfo`）へ変更（内部APIの変更）。

> 補足: 次バージョンでは、インクルードファイル（`#include "..."`）を辿ってグローバル変数の型と定義位置を解決する機能を予定しています。本バージョンは同一ファイル内の定義に対応します。

## [2.0.1] - 2026-07-27
### 修正
* 型名バッジの幅が型名の文字数に応じて変わり、変数名の左端が縦に揃わない不具合を修正（例: `int` と `float` で幅が異なっていた）。型名によらず幅 `140px` に固定するよう戻した。
  * v1.12.0 で `flex: 0 0 140px` として実装されていた固定幅が、v1.14.0 のレスポンシブ対応の際に `max-width: 140px` へ置き換わったことによる回帰。
  * パネル幅が 400px 以下の場合は、従来どおり型名を変数名の上へ折り返して表示します（固定幅を解除）。

## [2.0.0] - 2026-07-27

解析ロジック全体の再構成（リファクタリング）を完了したメジャーリリースです。
v1.16.0 から本バージョンまでの一連の作業により、テスト基盤の整備・既知バグの解消・内部構造の刷新を行いました。

### 修正
* 関数名を値として参照した際（関数ポインタへの代入など）に、グローバル変数（入力変数）と誤分類される不具合を修正。原因の異なる2件に対応。
  * 呼び出しより前に参照された場合、「呼び出し関数」と「入力変数」の両方に重複表示されていた（走査を2パスに分割して解消）。
  * 参照するだけで呼び出さない場合、入力変数として表示されていた（ファイル内の関数名を収集して除外）。

### 変更
* Webview の HTML 生成を `webviewHtml.ts`、スタイル定義を `webviewStyles.ts` へ分離。`webview.ts` はパネル管理とエディタ操作に専念する構成とした（621行 → 165行）。
* Webview に Content-Security-Policy（`default-src 'none'` + nonce）を追加。
* ハイライト対象の判定を、表示文字列の比較から `highlightable` フラグによるデータ判定へ変更。到達不能だった判定条件を削除。
* Webview 内のクリック処理を `data-name` 属性の参照に統一し、二重定義されていた名前の正規化処理を1箇所に集約。
* `analyzeCFunction` を解析フェーズ単位の5関数に分割（`collectFileScopeVars` / `findFunctionAtCursor` / `parseSignature` / `analyzeBody` / `buildResult`）。470行の単一関数が19行になり、技術仕様書のフェーズ構成と1対1で対応するようになった。
* カーソル位置の関数同定を、AST全走査から `descendantForPosition` と祖先探索による方式へ変更。
* `docs/analysis_spec.md` を現在の実装に同期（フェーズと関数名の対応表、2パス構成、Webview描画の章を追加）。

### 開発基盤
* 自動テストを 79 件に拡充（解析ロジック 50件 / ハイライト 11件 / HTML生成 18件）。過去の不具合修正を回帰テストとして明文化しており、各テスト名に確定バージョンを併記している。
* 型チェック（`npm run typecheck`）を追加し、`npm test` 実行時に自動で走るようにした。

> 補足: 本リリースは内部構造の刷新が中心です。利用者から見た機能・操作方法に変更はありません。

## [1.16.1] - 2026-07-27
### 修正
* 引数名が型名の一部と一致する場合（例: `struct data data`）に、引数の型名が `struct` のように途中で切れてしまう不具合を修正。型テキストを文字列検索ではなく構文ノードの位置から切り出すように変更。
* ローカル変数のインライン構造体宣言（例: `struct { int x; } s;`）で、型名に波括弧が含まれて表示が崩れる不具合を修正（ファイルスコープ側と同じ型名クレンジングを適用）。
* 関数ポインタを返す関数（例: `void (*get_handler(int id))(char *msg)`）を解析した際、外側の引数リスト（`char *msg`）を誤って読み取っていた不具合を修正。関数名に最も近い `function_declarator` の引数リストを採用するよう変更。
* 関数ポインタを引数に取る関数（例: `void f(int (*cb)(int))`）で、引数名が解決できず空欄の項目が生成される不具合を修正。
* ファイルスコープで宣言された関数ポインタ変数（例: `int (*handler)(int);`）の型情報が取得できず `global (推定)` と表示される不具合を修正。関数プロトタイプ宣言との判別をポインタ深さで行うよう変更。

### 変更
* 宣言子（declarator）の解析処理を `resolveDeclarator` に統合し、4箇所に重複していた探索ロジックを1つにまとめた（分岐の網羅度の不一致も解消）。
* 変数宣言のスキャン処理を `collectDeclaredVars` に統合し、ファイルスコープとローカルで重複していた処理をまとめた。
* グローバル変数のマクロ分類処理を `classifyGlobalVars` に統合した。
* ポインタ引数の型表記を、ポインタ深さに基づく形へ正規化（`int *` → `int*`、多重ポインタは `int **` → `int**`）。
* 型チェック（`npm run typecheck`）を追加し、`npm test` の実行時に自動で走るようにした。テストコードも型チェックの対象に含めた。

## [1.16.0] - 2026-07-27
### 追加
* テスト基盤を整備。Node標準の `node:test` による自動テスト（計53件）を追加し、`npm test` で実行できるようにした（追加パッケージなし）。
  * 過去のバージョンで修正した不具合をすべて回帰テストとして明文化（各テスト名に確定バージョンを併記）。
  * 未修正の既知バグ4件（引数型名の取得、ローカルのインライン構造体型名、識別子分類の走査順序依存、関数ポインタを返す関数の引数リスト）を `todo` マーク付きテストとして記録。
### 変更
* ハイライト検索用の正規表現生成処理を `src/highlight.ts` に純関数として分離し、VS Code に依存せずテスト可能にした。

> 補足: 本リリースは開発基盤の整備と内部構造の変更が中心であり、**拡張機能としての動作に変更はありません**。
> 本リリースは解析ロジック再構成（リファクタリング）の第1段階にあたり、一連の再構成が完了した時点で v2.0.0 とする予定です。

## [1.15.1] - 2026-07-26
### 修正
* 配列アクセスのみの項目（例: `hoge[]`）をクリックしてもエディタ上の該当箇所がハイライトされない不具合を修正。添字の閉じ括弧 `]` の直後は非単語文字（空白や `;`, `=` など）が続くことが多く、単語境界 `\b` が成立せず正規表現全体が一致しなかったことが原因。

## [1.15.0] - 2026-07-26
### 機能追加
* 構造体メンバアクセス（例: `var_ptr->sub.member`）および配列アクセス（例: `hogestruct[].a`, `grid[][]`）において、入出力変数の表示名にメンバ・アクセスパスを含めるよう改善。
* Webview 上でメンバ単位項目（`hogestruct[].a`）をクリックした際、エディタ上の該当箇所（`hogestruct[0].a` や `hogestruct[i].a`）を正確にハイライトするよう対応。

## [1.14.0] - 2026-07-26
### 改善
* Webview パネルの幅を狭くした際にUIが崩れる問題を修正。レスポンシブ対応を追加し、狭い幅でも型バッジ・変数名・コピーボタンが重ならないよう改善。
* コピーボタンを連続クリックした際に「完了」表示のまま元に戻らなくなるバグを修正。

## [1.13.4] - 2026-06-27
### 修正
* グローバル変数として定義された配列や構造体配列（例: `hogestruct[0].a = 100;`）への書き込み（代入）が、出力変数（Outputs）に分類されず検出漏れしていた不具合を修正。

## [1.13.3] - 2026-06-27
### 修正
* 多次元配列、ネストされた構造体、多重ポインタなどへの書き込み（代入）が「出力変数」に正しく検出されない不具合を、構文木を再帰的に解決するロジックを導入して修正。
* 複合代入演算子（`+=` など）およびインクリメント・デクリメント（`++`, `--`）を伴う書き込みに対し、「入力変数」としてもカウントするよう判定ロジックを改善。
* グローバル変数スキャン時にインライン構造体定義が含まれる場合、余分な `{...}` 部をカットして型名バッジの表示崩れを防ぐよう改善。
* Webview 上に埋め込むすべての変数名・型名に HTML エスケープ処理（XSSおよび表示崩れ対策）を追加。
* エディタ上に実体のない `戻り値 (return)` などの項目がクリックされた際、不要なハイライト検索をスキップする早期リターン処理を追加。

## [1.13.2] - 2026-06-25
### 修正
* `static void` や `inline void` のような修飾子付き void 戻り値関数を解析した際に、出力変数リストに誤って「戻り値 (return)」が追加表示されてしまう不具合を修正。

## [1.13.1] - 2026-06-18
### 変更
* 解析結果画面（Webview）のヘッダー部分から、関数名横に表示されていた戻り値の型名表示を削除。

## [1.13.0] - 2026-06-17
### 追加
* ファイル直下の変数宣言を事前にスキャンし、ファイル内定義のグローバル変数の型情報（`int` や `float` など）を自動適用してWebviewの型名バッジに表示する機能を追加。
  * ファイル内で定義が見つからないグローバル変数については、従来通り `global (推定)` として表示します。

## [1.12.1] - 2026-06-17
### 変更
* 推定されたグローバル変数の型表示エリアの文言を `extern/global (推定)` から `global (推定)` に変更。

## [1.12.0] - 2026-06-17
### 変更
* 変数リストの表示順序を「変数名 ➔ 型名」から「型名 ➔ 変数名」に入れ替え。
* 型名バッジの幅を固定（`140px`）に統一し、変数名の左端が縦に綺麗に揃うようにレイアウトを改善。

## [1.11.1] - 2026-06-17
### 修正
* 代入式の左辺（書き込み先）として使われているグローバル変数やポインタ引数が、誤って「入力変数（読み取り）」にも分類されて重複表示されてしまうバグを修正（Tree-sitterノードのオブジェクト比較方法のバグを修正）。

## [1.11.0] - 2026-06-16
### 追加
* 各変数および呼び出し関数の右側に個別の「コピー」ボタンを追加。クリックすると変数名または関数名のみ（例: `hoge`）を直接クリップボードにコピーできる機能を追加。
* 個別コピーボタンは、変数行にホバーした際にのみ表示されるスタイリッシュなUI設計に。
### 変更
* カード（カテゴリ見出し）の右側に配置されていた「一括コピー」ボタンをすべて削除。

## [1.10.1] - 2026-06-16
### 修正
* Webview 内のセキュリティ制約（セキュアコンテキスト制限）により、一括コピーボタンを押した際にクリップボードへのコピーが機能しない不具合を修正。コピー処理を Webview から VS Code 本体（拡張機能ホスト側）の API にメッセージ委譲するように改善。

## [1.10.0] - 2026-06-16
### 追加
* 各カテゴリ（入力変数、出力変数、内部変数など）のカードの右上に、そのカテゴリ内の変数リストを一括でクリップボードにコピーできる「コピー」ボタンを追加。
* コピー成功時に「コピー完了」と表示し、一時的にボタンをテーマカラーで強調するインタラクティブなフィードバックを追加。

## [1.9.0] - 2026-06-16
### 追加
* 各分類の見出しの右側に、そのカテゴリに検出された変数や関数の総数を示すカウント数バッジを表示する機能を追加。

## [1.8.0] - 2026-06-16
### 変更
* 変数が「入力」と「出力」の両方の条件に合致する場合（読み取りと書き込みが両方行われているポインタ引数やグローバル変数）、これまでは「出力」のみに表示されていた仕様を変更し、「入力変数」と「出力変数」の両方のリストに重複して表示されるように改善。

## [1.7.1] - 2026-06-16
### 修正
* Webview を閉じることなく複数の関数を連続で解析した際、変数を選択したときのハイライト対象が直近の関数ではなく、最初に解析した関数の行範囲のままになってしまうデータ同期バグを修正。
### 変更
* 各分類の見出し下部に表示されているカテゴリ別のアクセントラインの長さを、見出し全体の幅（100%）に引き伸ばし。

## [1.7.0] - 2026-06-15
### 変更
* 解析結果画面（Webview）のUIデザインを、視覚的アクセントを施したモダンなカード型レイアウトに刷新。
* カテゴリごとに個別のテーマカラーを設定し、情報のスキャナビリティ（視認性）を大幅に向上。
* 変数名の下に表示されていた分類詳細（例: 「入力引数（値渡し）」など）を削除し、UIをよりシンプルに整理。
* 変数の型名表示を、薄い半透明背景を用いた「バッジ」スタイルに統一。
* 変数や関数名にマウスホバーした際、右へ滑らかにスライドするマイクロインタラクション（ホバーエフェクト）を追加。
* データ未検出時の表示エリア（プレースホルダー）を、クリーンな破線枠のデザインに改善。

## [1.6.0] - 2026-06-15
### 追加
* Webview 上の変数名をクリックした際、コード上の配列（例: `hoge[i]`）や構造体（例: `hoge.A`, `ptr->member`）全体のアクセス表現もカバーして強調表示（ハイライト）される機能を追加。

## [1.5.2] - 2026-06-14
### 修正
* 関数ポインタ変数（例: `math_func`）の宣言を正しくパースして「内部変数（ローカル変数）」に分類されるように修正。
* 関数ポインタによる呼び出しが「入力変数」に誤検出されてしまう不具合を修正。
* 関数ポインタによる呼び出しを「呼び出し関数」リストから除外。

## [1.5.1] - 2026-06-14
### 修正
* Webview からのフォーカス移動時に、エディタ上の強調表示（ハイライト）が即座にクリアされてしまう不具合を修正。
* Webview 操作時にアクティブエディタが見失われ、強調表示（ハイライト）が適用されない問題を修正（複数エディタから解析対象ファイルを正しく探索するように改善）。

## [1.5.0] - 2026-06-14
### 追加
* 解析結果画面（Webview）上の変数名・関数名をクリックした際に、コード上の該当シンボルを一時的に強調表示（ハイライト）し、画面中央へ自動スクロールする機能を追加。
* エディタ上でカーソルを移動させたりテキストを編集したりした際に、強調表示（ハイライト）が自動で解除される機能を追加。

## [1.4.1] - 2026-06-14
### 変更
* 解析結果画面（Webview）における関数名・マクロ関数名の表示デザイン（レイアウト・下破線・ホバーエフェクト等）を変数名と統一。

## [1.4.0] - 2026-06-14
### 変更
* 解析結果画面（Webview）の各分類見出しから英語表記（`(Inputs)` など）を削除。
* 解析結果画面（Webview）の表示順序を、「呼び出し関数」の直後に「マクロ関数」が来るように変更。

## [1.3.0] - 2026-06-14
### 追加
* 変数名または関数名がすべて大文字のとき、通常の入出力や呼び出し関数とは別の分類として「マクロ変数 (Macro Variables)」および「マクロ関数 (Macro Functions)」のセクションに表示する機能を追加。
* 上記の大文字マクロ分類機能のオン・オフを切り替える設定項目（`c-function-analyzer.classifyAllUppercaseAsMacros`）を VS Code 設定に追加。

## [1.2.0] - 2026-06-09
### 変更
* プロジェクト内の識別子・コマンドID表記を `c-function-analyzer` に統一。
* 解析結果画面（Webview）のレイアウトを、ウィンドウ幅に関わらず常に縦1列で表示するように変更。
* 不要な開発ドキュメント（`requirements.md`、`implementation_plan.md`）を削除。

## [1.1.1] - 2026-06-06
### 修正
* 配列引数（例: `int a[]`）に対して配列書き込み（例: `a[5] = 10;`）を行った際、入力変数として誤認識されたり、グローバル変数（出力）として誤検出されたりするバグを修正。
* ポインタ引数の型表記において、型名に `*` が二重に付与されてしまう問題（例: `int **`）を修正。

## [1.1.0] - 2026-06-06
### 変更
* 解析結果画面（Webview）のUIを、VS Codeの標準テーマに適合するシンプルなフラットデザインに変更。

## [1.0.1] - 2026-06-06
### 修正
* `web-tree-sitter` のインポート処理を CommonJS（CJS）向けに名前付きインポートへ修正し、`init is not a function` のエラーを解決。
* `web-tree-sitter` を `0.20.8` にダウングレードし、WASM（C言語パーサー）との ABI バージョン不一致によるロードエラーを解決。
* パッケージ（VSIX）内の不要な旧 WASM ファイル（`web-tree-sitter.wasm`）および開発用デバッグログを削除（パッケージサイズを約 288 KB へ削減）。

## [1.0.0] - 2026-06-05
### 追加
* 初期リリース（C言語関数の静的解析拡張機能）。
* `web-tree-sitter` による AST 解析の実装（引数・ポインタ出力・グローバル変数・直接関数呼び出しの抽出）。
* VS Code テーマ適応型の Webview 解析結果画面の提供。
* ショートカットキー `Ctrl + Alt + A` での起動サポートを追加（C言語エディタ用）。
